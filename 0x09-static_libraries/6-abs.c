#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @n: integar
 *
 * Return: absolute value of an integar
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
