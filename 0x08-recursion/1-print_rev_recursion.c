#include "main.h"
/**
 * Reverse recursion - print a string in reverse
 * @n - string
 * Return -Always 0
 */

void _print_rev_recursion(char *s)
{
	if (*n)
	{
		_print_rev_recursion(n + 1);
		_putcher(*n);
	}
}
